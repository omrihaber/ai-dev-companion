[alembic]
script_location = migrations
# Placeholder only — migrations/env.py overrides this with Settings.database_url (ADC_DATABASE_URL).
sqlalchemy.url = postgresql+asyncpg://adc:adc@localhost:5432/adc
